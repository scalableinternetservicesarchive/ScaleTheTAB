
<!-- <p id="notice"><%= notice %></p>
	<br/><br/><br/> -->


	<div class="row">
		<% if owner_signed_in? %>
		<% page_header 'Your Restaurants'%>
		<div align="right">
			<%= button_to "Add Restaurant", new_restaurant_path, :method => "get", :class => " btn-custom btn", :style =>"margin-top:-120px" %>
		</div>
		<% else %>

		
			<div class = "col-xs-6 col-md-4" align="left">
				<% page_header 'Restaurants'%>
			</div>
			<div class="col-xs-12 col-md-8" align="right" style = "margin-top: -60px">
				<%= form_tag(restaurants_path, :method => "get", id: "search-form", :class => "form-inline") do %>
				<%= text_field_tag :search_name, params[:search_name], placeholder: "Search by Name", :class => "form-control", :style => "width:20%" %>
				<%= text_field_tag :search_city, params[:search_city], placeholder: "Search by City", :class => "form-control", :style => "width:20%" %> 
				<%= text_field_tag :search_zip_code, params[:search_zip_code], placeholder: "Search by Zip Code", :class => "form-control", :style => "width:22%" %>
				<%= submit_tag "Search", :class => "btn-custom btn" %> 
				<% end %>
			</div>
		
		<% end %>
	</div>



	<div align="center" style="margin-top: 50px">
		<%= will_paginate @restaurants %>
	</div>

	<% counter = 0 %>
	<div class = "row">
		<% @restaurants.each do |r| %>

		<% if counter % 3 == 0 and counter != 0 %>
	</div>
	<br>
	<div class = "row">
	<% @restaurants.each do |r| %>
  	<% if not owner_signed_in? or (owner_signed_in? and current_owner.id == r.owner_id) %>
    
    <% if counter % 3 == 0 and counter != 0 %>
    	</div>
    	<br>
    	<div class = "row">
    <% end %>	
    	<% cache(cache_key_for_restaurants_row(r)) do %>
    	<div class="col-xs-6 col-md-4">
    			<p><%= link_to r.name , r, :class => "link", :style => "font-size: 150%" %></p>
    <%=image_tag r.image.url(:medium) %>
    	</div>
	<% end %>
    <% counter = counter + 1%>
    <% end %>
	<% end %>
	</div>








