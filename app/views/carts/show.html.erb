<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h2>Your tab</h2>
<ul>
  <% @cart.line_items.each do |litem| %>
    <li><%= litem.quantity %> &times; <%= litem.item.title %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
