<div class="row" align="center">
<div class="border-form-div">
  <h2>New Table</h2><hr/>
  <%= form_for(@table) do |f| %>
  <% if @table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@table.errors.count, "error") %> prohibited this table from being saved:</h2>

      <ul>
      <% @table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_field :name, :class => "form-control", :placeholder => "Table Name" %>
  </div>
  <div class="form-group">
    <%= f.select :restaurant_id, Restaurant.all.collect {|x| [x.name , x.id]} , {:include_blank => "Select Restaurant"} %>
  </div>
  
  <%= f.submit 'Create Table', :class => 'btn-custom btn'%>
  
<% end %>

</div>
</div>

