<p id="notice"><%= notice %></p>

<h2>Your tab</h2>
<table>
  <%= @tab.carts.each do |cart| %>
    <tr>
      <td><%= render(cart) %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Edit', edit_tab_path(@tab) %> |
<%= link_to 'Back', tabs_path %>
